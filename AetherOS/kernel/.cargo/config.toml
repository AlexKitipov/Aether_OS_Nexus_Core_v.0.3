[build]
# Allow building from `AetherOS/kernel` without requiring an explicit --target.
target = "../.cargo/aetheros-x86_64.json"

[env]
# Keep host shell flags from leaking into dependency builds.
RUSTFLAGS = { value = "", force = true }
CARGO_ENCODED_RUSTFLAGS = { value = "", force = true }

[unstable]
build-std = ["core", "alloc", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]
