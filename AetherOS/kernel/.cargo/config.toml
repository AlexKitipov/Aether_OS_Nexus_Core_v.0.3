[build]
# Allow building from `AetherOS/kernel` without requiring an explicit --target.
target = "../.cargo/aetheros-x86_64.json"

[alias]
# Compatibility alias for users that still invoke `cargo bootimage`.
bootimage = "build --release --target ../.cargo/aetheros-x86_64.json -Zbuild-std=core,alloc,compiler_builtins -Zbuild-std-features=compiler-builtins-mem"

[env]
# Keep host shell flags from leaking into dependency builds.
RUSTFLAGS = { value = "", force = true }
CARGO_ENCODED_RUSTFLAGS = { value = "", force = true }
RUST_TARGET_PATH = { value = "../.cargo", force = true, relative = true }

[unstable]
build-std = ["core", "alloc", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]
