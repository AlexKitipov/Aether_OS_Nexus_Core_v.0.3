[build]
target = ".cargo/aetheros-x86_64.json"

[alias]
# Compatibility alias for users that still invoke `cargo bootimage`.
# Keep `--release` out of the alias so callers can explicitly select profile
# without triggering duplicate-flag errors (`cargo bootimage --release`).
# This workspace uses `bootloader_api` and direct kernel builds.
bootimage = "build --target .cargo/aetheros-x86_64.json -Zbuild-std=core,alloc,compiler_builtins -Zbuild-std-features=compiler-builtins-mem"

[env]
# Prevent host shell/compiler wrapper flags from leaking into dependency builds.
# In particular, `-Z no-implicit-prelude` causes cascading missing `Option` /
# `Result` errors in crates like serde_core.
RUSTFLAGS = { value = "", force = true }
CARGO_ENCODED_RUSTFLAGS = { value = "", force = true }
RUST_TARGET_PATH = { value = ".cargo", force = true, relative = true }

[unstable]
build-std = ["core", "alloc", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]
